[package]
name = "desktop-label-app"
version = "0.1.0"
description = "Skywin Labels — product list and label printing, talking to the same server the web app and Mac/Android clients use."
authors = ["Skywin Biotech"]
edition = "2021"

[lib]
name = "desktop_label_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "fs", "process"] }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Graphics_Printing",
    # OpenPrinterW is gated behind this feature in windows-rs 0.58,
    # despite having nothing to do with GDI itself — confirmed by a
    # real compile error on GitHub's windows-latest runner, not a guess.
    "Win32_Graphics_Gdi",
    "Win32_Foundation",
] }

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
